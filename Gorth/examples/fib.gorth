include "std/std.gorth"

// Print all Fibbonacci numbers in range [1, 100]

const N 100 end

func main
  : int
do
  1 1 while over N <= do
    over println_num
    swap over +
  end
  drop drop
  0
end
